# Database Configuration
DATABASE_URL=mysql://username:password@localhost:3306/database_name

# Server Configuration
APP_HOST=0.0.0.0
APP_PORT=8080

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-make-sure-it-is-at-least-256-bits-long-for-security
JWT_EXPIRE_HOURS=24
BCRYPT_COST=10  # development: 10, production: 12

# CORS Configuration
# Comma-separated list of allowed origins
# 
# ⚠️  IMPORTANT: Choose ONE of the following options:
#
# Option 1: Wildcard - Allow ALL origins (NOT for production!)
#           Note: This disables credentials (cookies/auth headers)
# CORS_ORIGINS=*
#
# Option 2: Development - Local development only
# CORS_ORIGINS=http://localhost:3000,http://localhost:5173,http://127.0.0.1:3000
#
# Option 3: Production with IP address (your server)
# CORS_ORIGINS=http://103.167.113.116:3000,http://103.167.113.116:5173,https://103.167.113.116:3000,https://103.167.113.116:5173
#
# Option 4: Production with domain name
# CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com,https://api.yourdomain.com
#
# Option 5: Mixed (development + production)
CORS_ORIGINS=http://localhost:3000,http://localhost:5173,http://127.0.0.1:3000,http://103.167.113.116:3000,http://103.167.113.116:5173

# Environment setting (development/production/staging)
ENVIRONMENT=development

# Rate Limiting Configuration (optional)
RATE_LIMIT_REQUESTS=60      # Requests per window
RATE_LIMIT_WINDOW_SECONDS=60  # Window duration in seconds

# Logging Configuration
RUST_LOG=debug  # development: debug, production: info or warn